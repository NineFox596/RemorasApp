import { StyleSheet } from 'react-native';

export const menuStyles = StyleSheet.create({
  container: {
    padding: 16,
  },

  title: {
    fontSize: 28,
    fontWeight: 'bold',
    marginBottom: 24,
  },

  button: {
    backgroundColor: '#2563eb',
    padding: 18,
    borderRadius: 14,
    marginBottom: 16,
    alignItems: 'center',
  },

  text: {
    color: '#fff',
    fontSize: 20,
  },
});
